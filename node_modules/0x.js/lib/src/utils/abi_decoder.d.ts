import * as Web3 from 'web3';
import { LogWithDecodedArgs, RawLog, ContractEventArgs } from '../types';
export declare class AbiDecoder {
    private savedABIs;
    private methodIds;
    constructor(abiArrays: Web3.AbiDefinition[][]);
    tryToDecodeLogOrNoop<ArgsType extends ContractEventArgs>(log: Web3.LogEntry): LogWithDecodedArgs<ArgsType> | RawLog;
    private addABI(abiArray);
    private padZeros(address);
}
